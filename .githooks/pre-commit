#!/usr/bin/env bash
powershell -ExecutionPolicy Bypass -File scripts/check-mojibake.ps1
status=$?
if [ $status -ne 0 ]; then
  echo "Commit bloqueado: corrija mojibake nas linhas staged."
  exit $status
fi